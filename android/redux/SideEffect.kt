
/**
 * @author Le Lin
 * @date 3/26/23
 */

/**
 * While this interface is empty, it is used to clearly define any Side effect that was caused by
 * specific [Action] on a given screen, Such as navigation, toast, animation etc that isn't change
 * the [State] and only get consumed once
 */
interface SideEffect